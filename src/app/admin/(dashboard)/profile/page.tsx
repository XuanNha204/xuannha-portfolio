import { ProfileManager } from "@/features/admin/profile/profile-manager";

export const metadata = { title: "Quản lý hồ sơ" };

export default function AdminProfilePage() {
  return <ProfileManager />;
}
