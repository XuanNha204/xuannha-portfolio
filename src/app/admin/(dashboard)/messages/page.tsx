import { MessageManager } from "@/features/admin/messages/message-manager";

export const metadata = { title: "Tin nhắn" };

export default function AdminMessagesPage() {
  return <MessageManager />;
}
